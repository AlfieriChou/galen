# `models rest`

> galenjs models rest.

## Usage

```javascript
const buildRouter = require('@galenjs/models-rest')

const prefix = '/v1'
const router = await buildRouter({ remoteMethods, prefix })
```
